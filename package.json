{
  "name": "monorepo-root",
  "private": true,
  "scripts": {
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "^9.1.7",
    "lint-staged": "^15.4.3"
  },
  "lint-staged": {
    "cd frontend && next lint --f*.{json,md,css,scss,js,jsx,ts,tsx}": [
      "prettier --write"
    ]
  }
}

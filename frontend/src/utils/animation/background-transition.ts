import { Variants } from 'framer-motion';

export const backgroundTransitionVariants: Variants = {
  initial: { backgroundColor: '#ffffff' },
  animate: { backgroundColor: '#f0f0f0', transition: { duration: 0.5 } },
};
